from django.shortcuts import render
from .models import *
#Create your views here.
# def sample_types(request):
#     sample_types = Sample.objects.all()
#     return render(request,'shopping/shopping.html',{'sample_types':sample_types})